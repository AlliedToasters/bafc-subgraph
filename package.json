{
  "name": "bafc",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ bafc",
    "create-local": "graph create --node http://localhost:8020/ bafc",
    "remove-local": "graph remove --node http://localhost:8020/ bafc",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 bafc"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.26.0",
    "@graphprotocol/graph-ts": "0.24.1"
  }
}
